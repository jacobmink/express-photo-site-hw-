<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/style.css">
    <title>Show Photo</title>
</head>
<body>
    <header>
        <h1><%= photo.title %></h1>
        <nav>
                <ul>
                    <li><a href="/users"><button>All Contributors</button></a></li>
                    <li><a href="/photos"><button>All Submissions</button></a></li>
                </ul>
            </nav>
    </header>
    <main>
        <section>
            Submitted by: <%= user.name %> <br>
            <img src="<%= photo.url %>" alt="<%= photo.title %>">
        </section>
       
        <section>
            <a href="/photos/<%= photo._id %>/edit"><button>Edit this photo</button></a><br>

            <form action="/photos/<%= photo._id %>?_method=DELETE" method="POST">
                <button type="submit">Delete this photo</button>
            </form>
        </section>
    </main>
    
</body>
</html>